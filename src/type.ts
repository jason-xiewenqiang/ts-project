let bool: boolean = false
let num: number = 1
let str: string = 'abc'

let arr1: number[] = [1, 2, 3]
let arr2: Array<string | number> = ['a', 'b', 'c', 1]

// 元组
let tuple: [number, string] = [1, 'abc']

let add = (x: number, y: number) => {
  return x + y
}

let compute: (x: number, y: number) => number

let obj : {x: number, y: number} = {x: 1, y: 1}

let sb: symbol = Symbol()

let sb1 = Symbol()

let ud: undefined = undefined

let nl: null = null

num = null


